{
  "name": "wplace-bluemarble",
  "version": "0.2.2",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "patch": "npm version patch --no-git-tag-version && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
