{
  "name": "wplace-bluemarble",
  "version": "0.73.3",
  "type": "module",
  "scripts": {
    "build": "node build/build.js",
    "patch": "node build/patch.js && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "terser": "^5.43.1"
  }
}
